version: '3'

includes:
  vars: 
    taskfile: TaskfileVars.yml
    flatten: true
  
tasks:
  release:
    desc: Create macOS release package
    deps:
      - build
    cmds:
      - ./DistributionTool -b -i {{.AppName}} -o {{.ReleaseDir}}