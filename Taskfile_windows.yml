version: '3'

includes:
  vars: 
    taskfile: TaskfileVars.yml
    flatten: true
  
tasks:
  release:
    desc: Create Windows release package
    deps:
      - build
    cmds:
      - ./DistributionTool.exe -b -i {{.AppName}} -o {{.ReleaseDir}}